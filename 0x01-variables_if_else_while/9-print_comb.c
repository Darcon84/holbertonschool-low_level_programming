#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
int x = 0;
while (x < 10)
{
putchar(a + '0');
putchar(',');
putchar(' ');
x++;
}
putchar('\n');
return (0);
}
